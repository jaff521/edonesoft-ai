# 发票开票 (INVOICE)

> 本文档为 ic-assistant 在处理「发票开票」事项时的业务参考文档。

---

## 办理逻辑

### 一、确认销方企业信息

- 复用 Step 1 中已查询的企业名称和统一社会信用代码作为销方信息
- 向客户确认是否正确

### 二、收集购方信息

逐步询问以下信息：

| 字段 | 必填 | 说明 |
|------|------|------|
| 购买方名称 | ✅ | 对方公司全称 |
| 购买方统一社会信用代码 | ❌ | 按以下优先级处理：**①** 若客户已主动提供，直接使用；**②** 若客户未提供且购方为企业（名称含"公司"、"有限"、"集团"等），助手**必须**自动调用 `python3 {baseDir}/scripts/unified_query.py "{购买方名称}"` 查询，查到后向客户展示并请客户确认无误后填入 `buyerCreditCode`；**③** 若查询不到，则留空不填 |
| 发票类型 | ✅ | 蓝字发票（正常开票）或红字发票（冲红） |
| 发票类别 | ✅ | 增值税专用发票（专票）或普通发票（普票） |
| 开票备注 | ❌ | 如项目名称、合同编号等 |

> [!IMPORTANT]
> **购方信用代码处理规则**：当购买方为企业且客户未主动提供信用代码时，助手**必须主动调用** `unified_query.py` 查询。若查到结果，须告知客户（例如："已为您查到购方的统一社会信用代码为：`91310114...`，请您确认是否正确？"），**客户确认后方可填入 `buyerCreditCode`**。若查询无结果，则留空提交即可，无需阻断流程。

### 三、收集开票明细行

逐行收集，每行包含：

| 字段 | 必填 | 说明 |
|------|------|------|
| 项目名称 | ✅ | 选定的标准税收分类名称（如"系统软件产品"、"工业自动控制系统"等） |
| 税收分类编码 | ✅ | **商品和服务税收分类编码（19位纯数字，必填）**。必须通过 `tax_query.py` 检索并由客户确认选择 |
| 规格型号 | ❌ | |
| 单位 | ❌ | 如"项"、"次"、"套"、"个"、"台"等。**单位提取规则**：当客户表述中包含了数量和单位（例如"1项"、"2次"、"5台"、"100套"），**必须准确提取出单位字符**（如"项"、"次"、"台"、"套"）填入 `unit` 字段并提交；若客户未提及单位，则 `unit` 留空不填（无需强行补充）。 |
| 数量 | ✅ | |
| 单价 | ✅ | **含税单价（元）**。工单要求提交含税价格。换算与确认规则见下方说明 |
| 税率 | ✅ | 常见值：6%（现代服务业）、13%（一般货物）、9%（运输/不动产）、3%（简易征收）、0%（免税） |

> [!IMPORTANT]
> **单价（unitPrice）含税价格确认与换算规则（强制步骤）**：
> 工单系统要求的单价必须为 **含税单价**。助手在收集单价时需遵循以下步骤：
> 1. **确认是否含税**：询问客户提供的单价是否已包含税费（例如：“请问您提供的单价 100 元是含税价格还是不含税价格？”）。
> 2. **含税单价换算**：
>    - 若客户确认**已是含税价**：直接使用该单价作为 `unitPrice`。
>    - 若客户表示是**不含税价**：按照公式 `含税单价 = 不含税单价 × (1 + 税率)` 计算（保留 2 位小数），并向客户进行确认。例如：不含税单价 100 元，税率 6%，计算得含税单价为 106.00 元。
>    - 提示话术：“您提供的单价 100 元为不含税价，按 6% 税率计算，含税单价为 106.00 元。请您确认是否按 106.00 元（含税价）提交？”
> 3. **客户确认锁定**：客户回复确认后，将换算后的含税单价填入 `unitPrice` 字段。

> [!IMPORTANT]
> **税收分类编码交互选择流程（强制步骤）**：
> 1. **提取关键字并查询**：提取客户给出的开票项目/关键字（如"软件开发"、"咨询费"），**必须**先调用查询脚本：
>    ```bash
>    python3 {baseDir}/scripts/tax_query.py "{关键字}"
>    ```
> 2. **展示候选列表供客户选择（项目名称格式化为 `*short_name*name`）**：
>    将返回的 `candidates` 列表以清晰的序号形式罗列给客户，项目名称**必须按 `*short_name*name` 格式化呈现**（如 `short_name` 存在则显示 `*short_name*name`，如 `*生产生活服务*软件维护服务`、`*软件*系统软件产品`；若 `short_name` 为空则直接使用 `name`）。也可直接读取 `tax_query.py` 返回的 `formattedName` 字段：
>    > 为您检索到以下匹配的税收分类标准项目，请选择对应序号：
>    > 1. *软件*系统软件产品（编码：`1060301010100000000`）
>    > 2. *生产生活服务*软件维护服务（编码：`3040201010000000000`）
>    > ...
> 3. **客户确认锁定**：客户选择序号（或确认具体项目）后，锁定该标准项目名称 `itemName`（填入真实的 `name`，如 `系统软件产品`）与 19 位 `goodsServiceTaxCode`。
> 4. **未检索到处理**：若返回列表为空，提示客户：“未找到相关的税收分类，请提供更通用或准确的关键字（例如‘软件’、‘咨询’、‘设计’等）”，收到新关键字后重新执行 `tax_query.py`，直到检索成功并经客户确认锁定时为止。

询问客户是否还有更多明细行，直到客户确认收集完毕。

### 四、汇总确认

将所有收集到的开票信息汇总展示给客户确认，格式示例：

> 开票信息汇总：
> 销方：上海星辰贸易有限公司
> 购方：北京某科技有限公司
> 购方信用代码：91110108MA01XXXXX（请核对）
> 类型：蓝字增值税专用发票
> 明细：
>   1. 系统软件产品（编码：1060301010100000000） × 1项 × 100000元，税率6%
>   2. 工业自动控制系统（编码：1090601020000000000） × 2次 × 5000元，税率6%
> 请确认以上信息无误。

---

## 字段结构规范

客户确认后，组装以下 JSON 调用 `invoice-creator` Skill：

```json
{
  "sessionKey": "{sessionKey}",
  "workOrder": {
    "enterpriseName": "{销方企业全名}",
    "creditCode": "{销方统一社会信用代码}"
  },
  "invoiceOrder": {
    "buyerName": "{购买方名称}",
    "buyerCreditCode": "{购买方信用代码}",
    "invoiceType": "{BLUE_INVOICE 或 RED_INVOICE}",
    "invoiceCategory": "{SPECIAL_VAT_INVOICE 或 NORMAL_INVOICE}",
    "invoiceRemark": "{开票备注}"
  },
  "invoiceDetailList": [
    {
      "itemName": "{标准项目名称}",
      "goodsServiceTaxCode": "{选定的19位税收分类编码}",
      "spec": "{规格型号}",
      "unit": "{单位}",
      "quantity": "{数量}",
      "unitPrice": "{单价}",
      "taxRate": "{税率}"
    }
  ]
}
```

### 字段映射说明

| 客户表述 | 字段值 |
|----------|--------|
| 蓝字发票 / 正常开票 | `invoiceType: "BLUE_INVOICE"` |
| 红字发票 / 冲红 | `invoiceType: "RED_INVOICE"` |
| 专票 / 增值税专用发票 | `invoiceCategory: "SPECIAL_VAT_INVOICE"` |
| 普票 / 普通发票 | `invoiceCategory: "NORMAL_INVOICE"` |

### 税收编码说明

- 明细行中的 `goodsServiceTaxCode` 为 **必填项（19位纯数字）**
- 必须在前置交互中通过 `tax_query.py` 检索并经客户选择确认后填入
- 严禁留空或未经客户确认直接提交

---

## 注意事项

1. 开票工单调用的 Skill 是 `invoice-creator`（不是 `ticket-creator`）
2. 开票工单创建成功后无 H5 确认页，回复模板为：
   > ✅ 开票工单已创建，工单号：`{ticket_id}`。请等待经办人确认后进行后续开票操作。
3. 如果客户同时选择了工商变更事项和发票开票，需要分别创建两个独立的工单
